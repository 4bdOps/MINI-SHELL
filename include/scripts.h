#ifndef SCRIPTS_H
#define SCRIPTS_H

/**
 * execute_script - Run a script file
 * @filename: Path to the script (e.g., "myscript.sh")
 * 
 * Reads the file line by line and executes each line.
 */
int execute_script(char *filename);

#endif
